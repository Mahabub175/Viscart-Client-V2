/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "thumbs.dreamstime.com",
      "anayase.com",
      "viscartapi.devmahabub.com",
    ],
  },
};

export default nextConfig;
